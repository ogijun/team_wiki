<h1>リビジョン #<%= @revision.id %></h1>
<% if @diff %>
  <h2>差分（#<%= @base.id %> → #<%= @revision.id %>）</h2>
  <div class="diff"><%= @diff %></div>
<% else %>
  <pre><%= @revision.body %></pre>
<% end %>
<p><%= link_to "履歴に戻る", page_revisions_path(@page) %></p>
