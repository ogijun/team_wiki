<% content_for :title, "文字起こしパート作成 — #{@material.title}" %>
<div class="nav-link"><%= link_to "← 資料へ戻る", @material %></div>
<div class="page-header"><h1>文字起こしパートを作成</h1></div>
<div class="muted"><%= @material.title %></div>
<div class="muted">この資料の音声・文字をテキストに書き起こします。パートに分けて複数人で分担できます。聞き取れた範囲でOK。</div>
<%= render "form" %>
