<% content_for :title, "文字起こし編集 — #{@material.title}" %>
<p><%= link_to "← 資料へ戻る", @material %></p>
<div class="page-header"><h1>文字起こしを編集</h1></div>
<p class="muted"><%= @material.title %> ・ <%= @transcription.display_label %></p>
<%= render "form" %>
